```
1) git clone git@github.com:Btcbel/daotokenization.git
2) cd preseed && npm install
3) touch mnemonic && echo 'YOUR_MNEMONIC' > mnemonic
4) ./node_modules/truffle/build/cli.bundled.js deploy --network mainnet
```
